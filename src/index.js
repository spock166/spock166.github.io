import React from 'react';
import './index.css';
import ReactDOM from 'react-dom/client';
import KevinDoesMath from './KevinDoesMath';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <KevinDoesMath/>
);
